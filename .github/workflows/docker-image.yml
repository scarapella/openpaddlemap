name: Create Docker Image

on:
    push:
        branches: ['none']
    pull_request:
        branches: ['none']

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v5
            - name: Build the Docker image
              run: docker build . --file Dockerfile --tag paddlemap:$(date +%s)
